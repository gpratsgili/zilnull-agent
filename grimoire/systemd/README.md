# Service Templates

In a full installation, service and timer templates live here.

The template keeps the directory as a placeholder so the repository shape matches the real project without bundling machine-specific service files.

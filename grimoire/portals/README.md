# Portals

Portal manifests live here in a full installation.

A portal is an always-on published surface. In the live system, portal manifests describe visible sections while runtime state lives under `vessel/state/portals/`.

If you are using a tailnet, you can set portals to live on always-on devices on an arbitrary domain, which can then be modified by spirits. This is very useful.
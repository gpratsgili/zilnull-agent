# grimoire/engine

This directory contains the ZIL⌀ runtime — the Python package that powers the harness.

The package is importable as `zil` (see `pyproject.toml`).

Spirit identity, prompts, and memories live at `spirits/zil/`, not here.
Spellbook manifests live at `grimoire/spellbooks/`.
This directory is internal machinery only.

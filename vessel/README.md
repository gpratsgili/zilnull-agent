# Vessel

The vessel is the machine-local operational container.

Use `vessel/state/` for runtime bookkeeping, `vessel/venvs/` for isolated helper runtimes, and `vessel/backups/` for local backup mirrors.

Keep shared world surfaces outside the vessel.
